CREATE TABLE dense_50000x20000_2500x1000_withrle (
X         INTEGER NOT NULL ENCODING RLE,
Y         INTEGER NOT NULL ENCODING RLE,
A1        INTEGER NOT NULL ENCODING GZIP_COMP,
PRIMARY KEY (X,Y)
);
